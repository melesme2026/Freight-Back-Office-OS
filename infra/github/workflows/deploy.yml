name: Deploy

on:
  workflow_dispatch:

jobs:
  deploy-placeholder:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deployment placeholder
        run: |
          echo "No automated deployment is configured yet."
          echo "Add real staging/production deployment steps before enabling push-based deploys."
          exit 1