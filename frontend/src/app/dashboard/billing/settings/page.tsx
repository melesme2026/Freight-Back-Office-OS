"use client";
import Link from "next/link";
import { useEffect, useState } from "react";
import { BillingModeNotice } from "@/components/billing/BillingModeNotice";
import { apiClient } from "@/lib/api-client";
import { getAccessToken, getOrganizationId } from "@/lib/auth";
type BillingSettings = { organization_id: string; plan_key: string; plan_code: string; subscription_status: string; billing_status: string; billing_provider: string; billing_notes: string | null; billing_enabled: boolean; payment_provider: string; supports_checkout: boolean; };
export default function BillingSettingsPage() { const [data, setData] = useState<BillingSettings | null>(null); const [error, setError] = useState<string | null>(null);
useEffect(() => { async function load() { const token = getAccessToken(); const organizationId = getOrganizationId(); if (!token || !organizationId) return; try { const response = await apiClient.get<{ data?: BillingSettings }>("/billing/settings", { token, organizationId }); setData(response?.data ?? null);} catch (err) { setError(err instanceof Error ? err.message : "Failed to load billing settings.");}} void load(); }, []);
return (<div className="mx-auto w-full max-w-5xl px-4 py-8 sm:px-6"><p className="text-sm font-medium text-brand-700">Dashboard / Billing / Settings</p><h1 className="mt-2 text-3xl font-bold text-slate-950">Billing settings</h1><p className="mt-2 text-sm text-slate-600">Owner-only billing foundation. No real payment collection actions are enabled in this release.</p><section className="mt-6"><BillingModeNotice /></section>{error ? <div className="mt-6 rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">{error}</div> : null}<div className="mt-6 grid gap-4 md:grid-cols-2"><article className="rounded-2xl border border-slate-200 bg-white p-5"><h2 className="text-base font-semibold text-slate-900">Subscription status</h2><dl className="mt-3 space-y-2 text-sm text-slate-700"><div className="flex justify-between gap-3"><dt>Plan</dt><dd className="font-medium">{data?.plan_code ?? "starter"}</dd></div><div className="flex justify-between gap-3"><dt>Plan key</dt><dd className="font-medium">{data?.plan_key ?? "starter"}</dd></div><div className="flex justify-between gap-3"><dt>Subscription</dt><dd className="font-medium">{data?.subscription_status ?? "trial"}</dd></div><div className="flex justify-between gap-3"><dt>Billing status</dt><dd className="font-medium">{data?.billing_status ?? "billing_not_configured"}</dd></div></dl></article><article className="rounded-2xl border border-slate-200 bg-white p-5"><h2 className="text-base font-semibold text-slate-900">Provider configuration</h2><dl className="mt-3 space-y-2 text-sm text-slate-700"><div className="flex justify-between gap-3"><dt>Billing enabled</dt><dd className="font-medium">{data?.billing_enabled ? "Yes" : "No"}</dd></div><div className="flex justify-between gap-3"><dt>Provider</dt><dd className="font-medium">{data?.billing_provider ?? "manual"}</dd></div><div className="flex justify-between gap-3"><dt>Payment provider</dt><dd className="font-medium">{data?.payment_provider ?? "none"}</dd></div><div className="flex justify-between gap-3"><dt>Checkout available</dt><dd className="font-medium">{data?.supports_checkout ? "Configured" : "Not configured"}</dd></div></dl></article></div><section className="mt-6 rounded-2xl border border-slate-200 bg-slate-50 p-5"><h2 className="text-base font-semibold text-slate-900">Plan actions</h2><p className="mt-2 text-sm text-slate-600">For trial, pilot, past_due, canceled, manual/offline billing, and billing_not_configured states, use assisted onboarding. No card data is collected here.</p><div className="mt-4 flex flex-wrap gap-3"><Link href="/pricing" className="rounded-xl border border-slate-300 bg-white px-4 py-2 text-sm font-semibold text-slate-700">Compare plans</Link><Link href="/request-demo?intent=upgrade" className="rounded-xl bg-brand-600 px-4 py-2 text-sm font-semibold text-white">Request plan change</Link><Link href="/request-demo?intent=contact-sales" className="rounded-xl border border-slate-300 bg-white px-4 py-2 text-sm font-semibold text-slate-700">Contact sales</Link></div></section></div>); }
